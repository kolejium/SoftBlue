/* function searchInStore (value: string) {

}

function search<T, U> (value: string, signal: AbortSignal, onSuccess : ((value : T) => void), onError : ((value : U) => void)) {
	const r = fetch('', {
		signal
	}).then(result => {

	},
	error => {

	});
}
*/
export {};
