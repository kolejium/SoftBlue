/* import { createSlice } from '@reduxjs/toolkit';

const themeSlice = createSlice({
	name: 'theme',
	initialState: false,
	reducers: {
		show: (state) => state = true,
		hide: (state) => state = false
	}
});

export const { show, hide } = themeSlice.actions;
*/
export {}; // default themeSlice.reducer;
